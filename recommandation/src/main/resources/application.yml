# ============================================================================
# 📄 파일: application.yml
# 위치: src/main/resources/application.yml
# ============================================================================

server:
  port: 8080

spring:
  application:
    name: recommendation-service

# Flask API 설정
recommendation:
  api:
    base-url: http://localhost:5002


# 로깅 설정
logging:
  level:
    com.Recipe.recommandation: INFO
    org.springframework.web: INFO